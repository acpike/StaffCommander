# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Dependencies (if we add them later)
node_modules/

# Build output (if we add build process later)
dist/
build/

# Environment files
.env
.env.local

# Assembled site + per-app build output (generated by scripts/build-site.sh)
_site/
gsp-3d/
*.tsbuildinfo
